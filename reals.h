#ifndef REALS_H
#define REALS_H

using real = double;
const real ZERO = 1e-6;
bool es_zero(real valor);
real max(real primer_valor, real segon_valor);
bool iguals(real primer_valor, real segon_valor);


#endif
